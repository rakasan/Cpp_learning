/* Advanced header files */
#include <string> /* String header file library */

//strlen() - function return length
//strcpy() - copy string to another
//strcmp() - compares two strings

#include <conio.h> /* console input output */
//clsr()  - clears the screen
// getch() - gets character from keyboard
#include <stdlib.h> /* Standard library header file */
//maloc() - allocated the memory block of given size and returns a pointer to the beginning of the block, but it does not initializes
//caloc() - allocates the memory and also initializes allocated memory block to 0

#include <math.h> /* Math header file */
//sqrt() - to obtain the square root
//pow() - to obtain power of a number

#include <ctype.h> /* Character type header file library */
// isalpha() - to find whether given character is an alphabet
// isdigit() - to find whether given character is a digit

#include <time.h> /* time header file library */
//setdate() - to modify the system date
//getdate() - to get CPU time

#include <assert.h> //assertion header file library
// assert() to perform assertion functions

#include <locale.h> //localization header file
//setlocale() - to set locale
//localeconv() - to get locale conventions

#include <signal.h>//signal header file library
//signal() - install signal handler
//raise() - to raise the signal

#include <setjmp.h> //jump header file library
#include <stdarg.h> //standard argument header file
//va_start() start of variable length argument list
//va_arg() fetch arguments from variable length argument

#include <errno.h> //error handling header file lib
//errno() - to indicate errors in program

#include <iomanip.h> // input output manipulation
//set()
//setprecission()

#include <fstream.h> // file stream header file library
